<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_gravity="bottom"
    android:orientation="vertical"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/small_margin"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:singleLine="true"
            android:textSize="@dimen/wallet_title_size"
            tools:text="Карта сбербанка"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/remove_wallet"
                android:layout_width="@dimen/wallet_button_size"
                android:layout_height="@dimen/wallet_button_size"
                android:layout_weight="1"
                app:srcCompat="@drawable/remove"/>

            <ImageView
                android:id="@+id/change_wallet"
                android:layout_width="@dimen/wallet_button_size"
                android:layout_height="@dimen/wallet_button_size"
                android:layout_marginLeft="8dp"
                android:layout_marginRight="8dp"
                app:srcCompat="@drawable/edit"/>

            <ImageView
                android:id="@+id/create_operation"
                android:layout_width="@dimen/wallet_button_size"
                android:layout_height="@dimen/wallet_button_size"
                android:layout_weight="1"
                app:srcCompat="@drawable/add"/>
        </LinearLayout>
    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:scrollbars="vertical"/>

</LinearLayout>